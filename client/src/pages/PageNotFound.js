import { NavLink } from "react-router-dom";

const PageNotFound = () => {
  return (
    <div>
      go back to Homepage, <NavLink to="/">Go Back</NavLink>
    </div>
  );
};

export default PageNotFound;
